import { PlayFieldCellDirective } from './play-field-cell.directive';

describe('PlayFieldCellDirective', () => {
  it('should create an instance', () => {
    const directive = new PlayFieldCellDirective();
    expect(directive).toBeTruthy();
  });
});
